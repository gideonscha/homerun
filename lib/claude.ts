// Phase 2 stub. The WhatsApp webhook will load a Claude client here, pass
// conversation history + contact_memory as context, and run the tool loop
// described in README.md ("Phase 2 architecture preview"). Leaving this as
// a placeholder so the import path is reserved.
export {};
