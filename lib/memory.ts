// Phase 2 stub. Per-contact memory layer — reads/writes the contact_memory
// table created in migration 002. Stubbed so callers can be wired in Phase 1
// without speculative code in the bundle yet.
export {};
